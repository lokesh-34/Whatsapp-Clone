/**
 * PushNotificationProvider — disabled stub.
 * Push notifications (Firebase FCM) are not configured.
 * This wrapper is kept to avoid removing it from main.jsx,
 * but does absolutely nothing — no SW registration, no console noise.
 */
export const PushNotificationProvider = ({ children }) => children